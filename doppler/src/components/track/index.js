import React, { Fragment } from 'react';

import './styles.css';

const Track = ({
    songName,
    key,
    version,
    lastModifyDate
}) => (
    <Fragment>
        <div className="track-container">
            <div>   </div>
        </div>
    </Fragment>

);

export default Track;